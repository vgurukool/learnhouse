apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "learnhouse.fullname" . }}-config
  labels:
    {{- include "learnhouse.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
